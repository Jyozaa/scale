# Local secrets — NEVER commit. CI reads these from GitHub secrets/vars.
# Copy to .env.local (gitignored) and fill in your key, or export in the shell.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.5-flash-lite
