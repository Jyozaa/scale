node_modules
.next
out
dist-test
dist-scripts
# Stale local compile outputs — sources of truth are the .ts files.
lib/*.js
*.log
.DS_Store
# Local secrets — the Gemini key must never be committed.
.env
.env.local
.env*.local
